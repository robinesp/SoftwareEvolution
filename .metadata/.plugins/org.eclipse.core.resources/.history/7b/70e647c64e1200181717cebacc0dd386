module Main

import IO;

set[str] wordsInFile(loc file) 
  = { word | /<word:[a-zA-Z]+>/ := readFile(file)};
  
 
void readRequirements() {
	file = |project://Assignment1/modis/asdad|;
}

void detect() {
	readRequirements();
}