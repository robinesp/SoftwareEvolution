module Main

import IO;

set[str] wordsInFile(loc file) 
  = { word | /<word:[a-zA-Z]+>/ := readFile(file)};
  
 
void readRequirements() {
	highReqs = |project://Assignment1/modis/high|.ls;
	lowReqs = |project://Assignment1/modis/low|.ls;
	
	print("<lowReqs>");
}

void detect() {
	readRequirements();
	print("ciao");
}